# Parser Fixtures

Store saved HTML or JSON payloads for:

- `autoxpress`
- `carzone`
- `carsireland`

These fixtures should be used to regression-test scraper selectors when public site markup changes.
